# 移除输入框弹窗中的价格预览

## 需求分析
- 用户希望输入框弹窗只显示斤数输入
- 移除单价和总价的预览显示
- 保持其他功能不变

## 当前代码结构
- ProductCard组件中包含输入框弹窗和价格预览
- 需要修改ProductCard组件的JSX部分
- 同时移除相关的CSS样式

## 实现方案

### 1. 修改ProductCard组件
- 移除pricePreview部分的JSX
- 保持输入框和按钮部分不变

### 2. 清理CSS样式（可选）
- 可以保留样式文件，避免潜在问题
- 或者移除不再使用的样式

## 文件修改清单
1. `src/components/ProductCard/index.tsx` - 移除价格预览部分

## 实现步骤
1. 修改ProductCard组件，删除价格预览JSX
2. 测试功能是否正常

## 保持不变的功能
- 输入框弹窗
- 斤数输入
- 实时计算（内部计算，但不显示）
- 取消和确认按钮
- 点击空白处关闭
